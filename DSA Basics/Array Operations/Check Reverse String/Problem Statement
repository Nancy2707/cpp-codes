Given two strings, determine if one is the reverse of the other. Return True if one string is the reverse of the other, otherwise return False.

Detailed explanation ( Input/output format, Notes, Images )
