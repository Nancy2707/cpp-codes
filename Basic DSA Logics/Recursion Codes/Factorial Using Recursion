#include<iostream> 
using namespace std;

void factorial(int n) {

    //base case
    if(n == 0)
        return 1 ;

return n * factorial(n - 1);
}

int main() {
    int n;
    cin >> n;

    cout << endl<<factorial(n);


    return 0;
}
